#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: arg count
 * @argv: vector of C strings
 * Return: int
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}

